<div class="auto__names">
    <ul>
        {% for brand in model %}
            <li><a href="{{ path('auto/brand', {'brand': brand.alias}) }}">{{ brand.name }}</a></li>
        {% endfor %}
    </ul>
</div>