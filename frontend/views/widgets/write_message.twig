<div id="write_message" class="form_box">
    <div class="header">
        <div class="title">Написать нам телеграмму</div>
        <div class="info">
            <div>У вас, видимо, имеется вопрос к нам, поэтому пишите, пишите нам:)<br /> Рады будем ответить!</div>
        </div>
        <!-- /.info -->
    </div>
    <!-- /.header -->
    <div class="body">
        {% set form = active_form_begin({
            'action': path('handler/action', {'action': 'write-message'})
        }) %}
            {{ form.field(modelForm, 'name').textInput({'placeholder': 'как к Вам обращаться?'}).label(false).error(false)| raw }}
            {{ form.field(modelForm, 'email').textInput({'placeholder': 'Ваш email'}).label(false).error(false)| raw }}
            {{ form.field(modelForm, 'info').textarea({'placeholder': 'дополнительная информация'}).label(false).error(false)| raw }}
            <div class="form-group submit_box">
                {{ html.submitButton('Отправить телеграмму', {'class': 'order_btn hvr-bounce-to-right'})| raw }}
            </div>
        {{ active_form_end() }}

    </div>
    <!-- /.body -->
</div>
<!-- /#write_message -->